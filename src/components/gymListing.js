import React from 'react';

const gymListing = ({gymName, street, height, width, length}) => {
    return (
        <div>
            <h1>{gymName}</h1>

        </div>
    )
}

export default gymListing;